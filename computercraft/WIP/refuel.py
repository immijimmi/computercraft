def try_refuel(move_sequence) -> bool:
    """
    If refuel fails (unable to refuel more than some function of len(move_sequence)),
    turtle should remain at refuel station and return False
    """

    backtrack(move_sequence)

    pass  #####
